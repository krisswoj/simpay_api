plugins {
    id 'java'
}

group 'pl.simpay'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {

    implementation(
            'com.google.code.gson:gson:2.8.1'
    )

    testCompile(
            group: 'junit', name: 'junit', version: '4.12'
    )
}
